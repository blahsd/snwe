/// <reference types="node" />
import * as net from 'net';
import { SocketWrapper } from 'mpc-js-core';
export declare class NodeSocketWrapper implements SocketWrapper {
    private socketFactory;
    private socket?;
    private deferred?;
    private socketListeners;
    constructor(socketFactory: () => net.Socket);
    connect(receive: (msg: string) => void, emit?: (eventName: string, arg?: any) => void): Promise<void>;
    send(msg: string): void;
    disconnect(): void;
}
