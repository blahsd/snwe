export declare class Mount {
    path: string;
    storage: string;
    constructor(valueMap: Map<string, string>);
}
export declare class Neighbor {
    neighbor: string;
    name: string;
    constructor(valueMap: Map<string, string>);
}
