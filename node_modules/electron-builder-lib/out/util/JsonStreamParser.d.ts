export declare class JsonStreamParser {
    private readonly eventHandler;
    private state;
    private buffer;
    private remainingMessageLength;
    constructor(eventHandler: (data: any) => void);
    parseIncoming(chunk: string): void;
}
