import { MPCCore } from 'mpc-js-core';
export declare class MPC extends MPCCore {
    connectTCP(hostname?: string, port?: number): Promise<void>;
    connectUnixSocket(path: string): Promise<void>;
}
